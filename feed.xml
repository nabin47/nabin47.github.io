<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://nabin47.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://nabin47.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-22T10:16:19+00:00</updated><id>https://nabin47.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal website of Jubair Ahmed Nabin </subtitle><entry><title type="html">From RNNs to Transformers: Why Attention Actually Works</title><link href="https://nabin47.github.io/blog/2026/from-rnns-to-transformers-why-attention-works/" rel="alternate" type="text/html" title="From RNNs to Transformers: Why Attention Actually Works"/><published>2026-07-14T00:00:00+00:00</published><updated>2026-07-14T00:00:00+00:00</updated><id>https://nabin47.github.io/blog/2026/from-rnns-to-transformers-why-attention-works</id><content type="html" xml:base="https://nabin47.github.io/blog/2026/from-rnns-to-transformers-why-attention-works/"><![CDATA[<h1 id="from-rnns-to-transformers-why-attention-actually-works">From RNNs to Transformers: Why Attention Actually Works</h1> <h2 id="why-this-matters">Why This Matters</h2> <p>For years, “transformers are better than RNNs” was something I accepted without really interrogating. It’s the kind of claim that gets repeated in papers, in lectures, in casual conversation, until it hardens into an assumption nobody re-derives. I taught it that way for a while too. But “better” is not an explanation, and if you’re going to teach a mechanism, you owe your students the actual reason it works — not just the verdict.</p> <p>This post is my attempt to lay out that reason properly, in one continuous argument rather than the fragments I originally worked through over several days. It starts with a genuine limitation of recurrent networks, follows the fix (attention) through increasing levels of sophistication — self-attention, cross-attention, multi-head attention — and ends with what I think is the single most useful sentence for holding all of it in your head at once.</p> <h2 id="the-problem-rnns-read-one-word-at-a-time">The Problem: RNNs Read One Word at a Time</h2> <p>Recurrent neural networks process a sequence the way a student reads a sentence with their finger — one word, then the next, carrying a running summary forward as they go. For short sentences, this works fine. Take:</p> <blockquote> <p>“The boy is hungry.”</p> </blockquote> <p><em>Boy</em> and <em>hungry</em> sit right next to each other. The dependency is trivial to capture: by the time the network reaches “hungry,” “boy” is still fresh in the running summary.</p> <p>Now stretch the sentence:</p> <blockquote> <p>“The boy who plays in the field every afternoon with his friends and has a bicycle is hungry.”</p> </blockquote> <p><em>Boy</em> and <em>hungry</em> are still the two words that matter to each other — grammatically, semantically, nothing has changed about their relationship. But now there’s a long chain of intervening words between them. An RNN has to carry the information about “boy” forward through every one of those intermediate steps, and at each step, that information gets compressed a little further into a fixed-size summary vector. By the time the network reaches “hungry,” the signal from “boy” has been squeezed through so many transformations that it’s substantially degraded.</p> <p>This is the core limitation: RNNs don’t have a mechanism for <em>directly</em> connecting two words based on their actual relevance to each other. They only have a shrinking, sequential summary.</p> <p>LSTMs (and GRUs) were built specifically to address this — gating mechanisms that let the network selectively preserve or discard information as it moves through the sequence. But it’s worth being precise about what they do: <strong>they patch the problem. They don’t fix it.</strong> The fundamental architecture is still step-by-step, still passing information forward through a bottleneck, just with better bookkeeping about what to keep. Long-range dependencies improve, but the structural issue — no direct line between distant relevant words — remains.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/rnn-transformer-example-480.webp 480w,/assets/img/rnn-transformer-example-800.webp 800w,/assets/img/rnn-transformer-example-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/rnn-transformer-example.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="RNN vs Transformer" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">RNN vs Transformers</figcaption> </figure> <h2 id="the-fix-attention-looks-back-directly">The Fix: Attention Looks Back Directly</h2> <p>Attention solves this by abandoning the sequential summary altogether. Instead of passing a compressed running state forward step by step, it lets the model look back at <em>every</em> word in the sequence directly, regardless of distance, and decide how much each one matters to the word currently being processed.</p> <p>Take the same long sentence. When the model processes “hungry,” it doesn’t rely on whatever survived the compression chain. It computes a direct relevance score against every other word in the sentence. Something like:</p> <table> <thead> <tr> <th>Word</th> <th>Attention weight</th> </tr> </thead> <tbody> <tr> <td>The</td> <td>0.03</td> </tr> <tr> <td>boy</td> <td>0.68</td> </tr> <tr> <td>is</td> <td>0.24</td> </tr> <tr> <td>hungry</td> <td>0.05</td> </tr> </tbody> </table> <p>The exact numbers here are illustrative, not measured [CITATION NEEDED if you want real weights from a trained model] — but the <em>pattern</em> is the point. “Boy” gets the overwhelming share of attention, because it’s the word that actually explains “hungry,” and the model doesn’t have to route that information through every intervening word to get it. Distance in the sentence stops being a cost.</p> <p>This is also, incidentally, why transformers train faster than RNNs on top of being more accurate on long-range dependencies: because attention computes relationships across the whole sequence in parallel, rather than being forced into the strictly sequential, one-step-at-a-time processing that RNNs require. That parallelism is a training-efficiency win as much as it is an accuracy win.</p> <p>The whole trick, distilled: <strong>stop passing a shrinking summary forward — just let the model look directly at what matters.</strong></p> <h2 id="self-attention-every-word-checks-in-with-every-other-word">Self-Attention: Every Word Checks In With Every Other Word</h2> <p>The mechanism described above, where words within a single sequence attend to each other, is called <strong>self-attention</strong>, and it’s worth sitting with how simple the underlying idea is, because the results it produces can feel disproportionate to the mechanism.</p> <p>Consider a classic ambiguous sentence:</p> <blockquote> <p>“The trophy didn’t fit in the suitcase because it was too big.”</p> </blockquote> <p>What does “it” refer to — the trophy or the suitcase? You resolve this instantly, without conscious effort. But it’s a real inference problem: “it” is grammatically ambiguous, and resolving it requires understanding the <em>semantics</em> of fitting one object inside another (if the trophy is too big, it’s the trophy that doesn’t fit; “it” refers back to the trophy).</p> <p>Self-attention gives the model a mechanism to work this out. The word “it” gets to look at every other word in the sentence and effectively ask, “how relevant are you to me, right now?” “Trophy” receives a high relevance score. “Suitcase” receives a lower one. “Because” barely registers. There’s no fixed order to this process, and no step-by-step memory chain being maintained — just every word checking in with every other word, simultaneously, and weighting the relationships accordingly.</p> <p>That’s the entire idea. It sounds almost too simple to produce the results it does — but stacked across many layers and scaled up across many parameters, this single mechanism is what allows transformer-based models to hold coherent context across entire paragraphs, not just sentences.</p> <h2 id="cross-attention-when-two-different-sequences-need-to-talk">Cross-Attention: When Two Different Sequences Need to Talk</h2> <p>Self-attention has a boundary, though: it only operates <em>within</em> one sequence. In a sentence, “dog” can attend to “frisbee” because they’re both part of the same sequence. But plenty of real tasks involve two <em>different</em> sequences that need to inform each other.</p> <p>The clearest example is image captioning. A model generating the caption “a dog catching a frisbee” is producing a sequence of words, but it needs to ground those words in a completely different kind of sequence — regions of an image. The words being generated and the image being described are not the same sequence at all. So how does the model know, at each word, where in the image to look?</p> <p>This is <strong>cross-attention</strong>: one sequence attends to a different sequence entirely. When the model writes “dog,” it attends heavily to the image region containing the dog. When it writes “frisbee,” attention shifts to a completely different region. It is not staring at the whole image uniformly and hoping the right features surface for every word — the attention target moves, word by word, in step with what’s being generated.</p> <p>I’ll admit this distinction wasn’t always obvious to me in an explicit way. I understood captioning models produced accurate captions, but I had implicitly assumed something closer to the model processing the whole image equally for every generated word. It took a student asking, essentially, “how does the model know where to look?” for me to realize I’d only ever explained the <em>static</em> version of attention — self-attention within one sequence — and never actually walked through the <em>dynamic</em>, cross-sequence version explicitly. That gap between the two is exactly what cross-attention closes.</p> <p>This is the mechanism underlying captioning, machine translation, and essentially any transformer task involving two distinct sequences (source and target). The line worth keeping: <strong>self-attention looks within; cross-attention looks across.</strong></p> <h2 id="multi-head-attention-asking-several-questions-at-once">Multi-Head Attention: Asking Several Questions at Once</h2> <p>There’s a further wrinkle. Even within a single sentence, self-attention alone can only chase one kind of relationship at a time per attention computation — but a sentence often raises more than one question simultaneously. Take:</p> <blockquote> <p>“The bank near the river closed.”</p> </blockquote> <p>Here, several distinct relationships are worth tracking at once: Is “bank” being used in the financial or geographical sense? What’s the main verb, and which word is its subject? Which words modify which? A single attention layer, computing one set of relevance scores, can’t cleanly capture all of these different relationship types simultaneously — they call for different lenses.</p> <p>This is what <strong>multi-head attention</strong> provides. I used to think of it, incorrectly, as running attention multiple times “just to be safe” — a kind of redundancy or ensembling for robustness. That’s not what’s happening. Each attention head specializes in tracking a <em>different type</em> of relationship, entirely on its own, without any explicit instruction about what it should focus on. One head might end up locking onto the “bank”/”river” disambiguation. Another might track subject-verb structure. Nobody assigns these roles in advance — no engineer writes “head 3, handle grammar.” These specializations <strong>emerge from training</strong> as a consequence of how the heads are optimized jointly.</p> <p>This clicked for me in a similar way to the cross-attention gap above — a student asked, quite reasonably, “how do we tell head 3 to look at grammar?” And the honest answer is: we don’t, and we can’t. It just happens, as an emergent property of gradient descent operating over many parallel attention computations. That’s a genuinely different claim from “we run attention several times for robustness,” and it’s worth being precise about the distinction when teaching this.</p> <p>The result is that transformers can hold <em>several</em> relationships in a sentence active simultaneously, rather than being forced to commit to just one. The line worth keeping here: <strong>multiple attention heads means multiple questions asked at the same time — not one question asked more carefully.</strong></p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/the-bank-near-river-example-480.webp 480w,/assets/img/the-bank-near-river-example-800.webp 800w,/assets/img/the-bank-near-river-example-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/the-bank-near-river-example.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="different heads, different jobs" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Different Heads, Different Jobs</figcaption> </figure> <h2 id="putting-it-together">Putting It Together</h2> <p>None of these ideas are individually exotic — that’s part of what makes the overall picture satisfying rather than intimidating. The arc is:</p> <ol> <li><strong>RNNs</strong> process sequentially and lose long-range information to compression, even with LSTM-style patches.</li> <li><strong>Self-attention</strong> replaces the sequential bottleneck by letting every word look directly at every other word in the same sequence, all at once.</li> <li><strong>Cross-attention</strong> extends this across two <em>different</em> sequences — letting one (say, generated text) pull targeted information from another (say, an image).</li> <li><strong>Multi-head attention</strong> runs several independent attention computations in parallel, each free to specialize in a different kind of relationship, with no explicit supervision on what those specializations should be.</li> </ol> <p>Stacked and scaled, this is the mechanism underneath essentially every modern large language model, image captioning system, and translation system currently in use.</p> <p>If there’s one discussion prompt worth sitting with, it’s this: multi-head attention’s specializations emerge without anyone assigning them. Where else — in your own reading of a sentence, or in your own work — have you noticed yourself implicitly holding two different interpretations open at once before consciously picking the right one? That kind of parallel, unassigned disambiguation is precisely what these models are doing computationally, and I think it’s a useful mirror to hold up to our own reading comprehension.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="deep-learning"/><category term="transformers"/><category term="attention-mechanism"/><category term="self-attention"/><category term="cross-attention"/><category term="multi-head-attention"/><category term="rnn"/><summary type="html"><![CDATA[A first-principles walkthrough of why transformers replaced RNNs — from the vanishing-context problem, through self-attention and cross-attention, to why multiple attention heads learn different jobs without ever being told to.]]></summary></entry><entry><title type="html">Travelling Salesperson HackerRank Solution with Algorithm</title><link href="https://nabin47.github.io/blog/2023/tsp-solution/" rel="alternate" type="text/html" title="Travelling Salesperson HackerRank Solution with Algorithm"/><published>2023-07-31T16:40:16+00:00</published><updated>2023-07-31T16:40:16+00:00</updated><id>https://nabin47.github.io/blog/2023/tsp-solution</id><content type="html" xml:base="https://nabin47.github.io/blog/2023/tsp-solution/"><![CDATA[<p>Travelling salesperson is a classic graph problem. Here, the cost of moving from city <code class="language-plaintext highlighter-rouge">i</code> to city <code class="language-plaintext highlighter-rouge">j</code> is given. The task is to find a path, starting from the city <code class="language-plaintext highlighter-rouge">0</code> encompassing all the cities at most once whose cost is the smallest.</p> <h2 id="problem-statement">Problem Statement:</h2> <p>Given a matrix <code class="language-plaintext highlighter-rouge">M</code> of size <code class="language-plaintext highlighter-rouge">N</code> where <code class="language-plaintext highlighter-rouge">M[i][j]</code> denotes the cost of moving from city <code class="language-plaintext highlighter-rouge">i</code> to city <code class="language-plaintext highlighter-rouge">j</code>. Your task is to complete a tour from the city <code class="language-plaintext highlighter-rouge">0</code> (0-based index) to all other cities such that you visit each city at most once and then at the end come back to the city <code class="language-plaintext highlighter-rouge">0</code> in min cost.</p> <p>Input Format</p> <p>The first line of input contains an integer T denoting the no of test cases. Then T test cases follow. Each test case contains an integer N denoting the size of the matrix then in the next line are N*N space separated values of the matrix M.</p> <p>Constraints</p> <p><code class="language-plaintext highlighter-rouge">1&lt;=T&lt;=15, 1&lt;=N&lt;=12, 1&lt;=M[][]&lt;10000</code></p> <p>Output Format</p> <p>For each test case print the required result denoting the min cost of the tour in a new line.</p> <p>Sample Input</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>220 111112 030 1000 50005000 0 10001000 5000 0
</code></pre></div></div> <p>Sample Output</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>2233000
</code></pre></div></div> <p>Input Visualization</p> <p><img src="/assets/input-visualization-travelling-salesman.png" alt="Input visualization through graph"/></p> <h2 id="algorithm">Algorithm</h2> <ul> <li>Take the graph input into a 2D matrix.</li> <li>Initialize a visited array of <code class="language-plaintext highlighter-rouge">n</code> sizes with <code class="language-plaintext highlighter-rouge">0</code>.</li> <li>Set <code class="language-plaintext highlighter-rouge">visited[0]</code> to <code class="language-plaintext highlighter-rouge">1</code>.</li> <li>Call traverse with arr, cur node (<code class="language-plaintext highlighter-rouge">0</code>), number of nodes visited (<code class="language-plaintext highlighter-rouge">cnt = 1</code>), total cost (<code class="language-plaintext highlighter-rouge">0</code>), Since already in <code class="language-plaintext highlighter-rouge">0th</code> node so the cost of going from <code class="language-plaintext highlighter-rouge">0th</code> node to <code class="language-plaintext highlighter-rouge">0th</code> node is <code class="language-plaintext highlighter-rouge">0</code>.</li> <li>Check if <code class="language-plaintext highlighter-rouge">cnt == n</code>. If true then we have visited all the nodes. Put the minimum of ans and ( total + cost of going from the current node to node 0) into <code class="language-plaintext highlighter-rouge">ans</code>. Then return.</li> <li>Iterate through all the nodes.</li> <li>If the <code class="language-plaintext highlighter-rouge">ith</code> node is not visited and it’s a valid node make <code class="language-plaintext highlighter-rouge">visited[cur]=1</code>. Call traverse by making ith node equal to <code class="language-plaintext highlighter-rouge">cur</code>, <code class="language-plaintext highlighter-rouge">cnt+1</code> and total+cost from cur to <code class="language-plaintext highlighter-rouge">ith</code> node (<code class="language-plaintext highlighter-rouge">arr[cur][i]</code>).</li> </ul> <h2 id="c-implementation">C++ Implementation</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#include &lt;iostream&gt;
#include &lt;vector&gt;
#include &lt;climits&gt;
using namespace std;

int ans = INT_MAX, n;

bool valid(int i) {
  return i &gt;= 0 &amp;&amp; i &lt; n;
}

void traverse(vector&lt;vector&lt;int&gt; &gt;&amp; arr, int cur, vector&lt;int&gt; visited, int cnt, int total) {
  // all nodes are visited
  if(cnt == n) {
    ans = min(ans, total + arr[cur][0]); // add the distance from last node to the 0th node
    return;
  }
  
  // explore all the paths that are not yet visited
  for(int i = 0; i &lt; n; i++) {
    if(!visited[i] &amp;&amp; valid(i)) {
      visited[i] = 1;
      traverse(arr, i, visited, cnt+1, total+arr[cur][i]);
      visited[i] = 0; // so that this node can be used in another sequence to check if min cost can be obtained
    }
  }
}

int main() {
  int t;
  cin&gt;&gt;t;
  
  while(t--) {
    cin&gt;&gt;n;
    vector&lt;vector&lt;int&gt; &gt; arr(n, vector&lt;int&gt;(n));
    vector&lt;int&gt; visited(n, 0);
    ans = INT_MAX;
    
    // take the graph input
    for(int i = 0; i &lt; n; i++) {
      for(int j = 0; j &lt; n; j++) {
        cin&gt;&gt;arr[i][j];
      }
    }
    
    visited[0] = 1;
    // check all possible path to find the min cost path
    traverse(arr, 0, visited, 1, 0); 
    
    cout&lt;&lt;ans&lt;&lt;endl;
  }
  return 0;
}
</code></pre></div></div> <p><strong>Problem link:</strong> <a href="https://www.hackerrank.com/contests/target-samsung-13-nov19/challenges/travelling-salesman-4">Travelling Salesman - Target Samsung 13 Nov’19</a> ——</p>]]></content><author><name></name></author><category term="problem-solving"/><category term="data-structures"/><category term="algorithms"/><summary type="html"><![CDATA[Travelling salesperson is a classic graph problem. Here, the cost of moving from city i to city j is given. The task is to find a path, starting from the city 0 encompassing all the cities at most once whose cost is the smallest.]]></summary></entry><entry><title type="html">How-Tos of Installing and Mounting a FUSE-based Filesystem</title><link href="https://nabin47.github.io/blog/2023/fuse-filesystem-installation-process/" rel="alternate" type="text/html" title="How-Tos of Installing and Mounting a FUSE-based Filesystem"/><published>2023-07-20T16:40:16+00:00</published><updated>2023-07-20T16:40:16+00:00</updated><id>https://nabin47.github.io/blog/2023/fuse-filesystem-installation-process</id><content type="html" xml:base="https://nabin47.github.io/blog/2023/fuse-filesystem-installation-process/"><![CDATA[<h2 id="what-exactly-is-fuse">What exactly is FUSE?</h2> <p>Before we get any further let’s clear up one thing FUSE is not the fuse that interrupts the circuit when the current becomes too strong. Now that the pun is out of the way let’s get started.</p> <p>FUSE or Filesystem in Userspace is a software interface for Unix and Unix-like computer operating systems that lets non-privileged users create their own file systems without editing kernel code<sup>1</sup>.</p> <p>Allowing secure, non-privileged mounts is one of FUSE’s most significant features. This broadens the scope of filesystem applications. Sshfs<sup>2</sup>, a secure network filesystem that uses the sftp protocol, is an excellent example.</p> <h2 id="premise">Premise</h2> <p>This article assumes you already have a filesystem written in Python. Now the next step is to mount it in your system (assuming it’s Unix-like OS). For Windows refer to <a href="https://github.com/winfsp/winfsp">WinFsp</a>. The article was written using Ubuntu 22.04.</p> <h2 id="installing-dependencies">Installing dependencies</h2> <p>To run a FUSE-based filesystem, first, we need to install the FUSE and fusepy modules. The corresponding steps and commands are as follows:</p> <ol> <li>Install FUSE: You can install FUSE module on Ubuntu using the following command:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install </span>fuse
</code></pre></div></div> <ol> <li>Install fusepy: You can install fusepy using pip, the package manager for Python. If pip is not installed, you can install it using the following command:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install </span>python3-pip
</code></pre></div></div> <ol> <li>Once you have pip installed, you can install fusepy by running the following command:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip3 <span class="nb">install </span>fusepy
</code></pre></div></div> <h2 id="mounting-the-file-system">Mounting the file-system</h2> <p>Now that we have installed the dependencies why not mount the filesystem to see what it does! Suppose you have the codes for the filesystem saved in a Python file with a <code class="language-plaintext highlighter-rouge">.py</code> extension, for example, <code class="language-plaintext highlighter-rouge">examplefs.py</code>.</p> <ol> <li>Run the code: To run the code, open a terminal and navigate to the directory where you saved the code and run the following command:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 entropyfs.py &lt;mountpoint&gt;
</code></pre></div></div> <ol> <li> <p>Replace <code class="language-plaintext highlighter-rouge">&lt;mountpoint&gt;</code> with the directory where you want to mount the file system. This directory must exist before you run the command.</p> </li> <li> <p>For example, if you want to mount the file system to the <code class="language-plaintext highlighter-rouge">~/testfs</code> directory, run the following command:</p> </li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 entropyfs.py ~/testfs
</code></pre></div></div> <ol> <li> <p>The file system is now mounted, and you can interact with it as you would any other file system.</p> </li> <li> <p>To see the list of all mounted filesystems run the following command</p> </li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mount
</code></pre></div></div> <h2 id="unmount-the-filesystem">Unmount the filesystem</h2> <p>To unmount the file system, you can use the <code class="language-plaintext highlighter-rouge">umount</code> command in the terminal.</p> <ol> <li>First, you need to navigate to the directory where you mounted the file system. For example, if you mounted the file system to <code class="language-plaintext highlighter-rouge">~/testfs</code>, you can navigate to it using the following command:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> ~/testfs
</code></pre></div></div> <ol> <li>Then, to unmount the file system, run the following command:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>umount &lt;mountpoint&gt;
</code></pre></div></div> <ol> <li> <p>Replace <code class="language-plaintext highlighter-rouge">&lt;mountpoint&gt;</code> with the directory where you mounted the file system. In this example, it would be <code class="language-plaintext highlighter-rouge">~/testfs</code>.</p> </li> <li> <p>For example, the command to unmount the file system mounted at <code class="language-plaintext highlighter-rouge">~/testfs</code> would be:</p> </li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>umount ~/entropyfs
</code></pre></div></div> <p>After running this command, the file system will be unmounted and you can no longer access it.</p>]]></content><author><name></name></author><category term="guides"/><category term="fuse-filesystem"/><category term="how-to-guide"/><summary type="html"><![CDATA[What exactly is FUSE?]]></summary></entry></feed>